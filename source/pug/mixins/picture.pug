mixin picture(data={})
  picture
    each source in data.list
      source(type="image/webp", media=source.media, srcset=source.srcset)
    //- 1x: data.width; 2x: 2*data.width
    img(src=data.src, alt=data.alt, width=data.width, height=data.height, loading="lazy")
